# dependencies.R
# Rhino Package Dependencies

# Core Shiny Framework
library(shiny)
library(bslib)
library(htmltools)

# Visualization
library(plotly)
library(leaflet)
library(DT)

# Data Processing
library(dplyr)
library(tidyr)
library(purrr)
library(stringr)
library(readr)
library(haven)

# API Access
library(httr)
library(jsonlite)

# UI Enhancement
library(waiter)

# Logging
library(logger)
